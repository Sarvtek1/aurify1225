name: PDPL Compliance Guard

on:
  pull_request:
  push:
    branches: [ master, main ]

jobs:
  guard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify Hosting rewrite points to me-central1
        run: |
          test -f firebase.json
          grep -q '"run"' firebase.json
          grep -q '"region": *"me-central1"' firebase.json
          echo "✔ firebase.json references me-central1 for dynamic routes"

      - name: Fail if frameworks backend key is present (we proxy to Cloud Run instead)
        run: |
          if grep -qi 'frameworksBackend' firebase.json; then
            echo "❌ frameworksBackend found; remove and use rewrites → Cloud Run me-central1"
            exit 1
          fi
          echo "✔ frameworksBackend not used"

      # Optional: verify docs mention the correct regions
      - name: Verify compliance docs reference Doha
        run: |
          test -f docs/compliance/pdpl-readiness.md
          grep -qi 'me-central1' docs/compliance/pdpl-readiness.md
          grep -qi 'ME-CENTRAL1' docs/compliance/pdpl-readiness.md
          echo "✔ compliance doc references me-central1 / ME-CENTRAL1"

  # (Optional) infra sanity checks – needs GCP auth using your existing secret
  infra:
    if: ${{ github.event_name == 'push' || github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    needs: guard
    steps:
      - uses: actions/checkout@v4

      - name: Auth to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AURIFY1225 }}

      - name: Set project
        run: gcloud config set project aurify1225

      # ✅ Non-fatal check: do NOT fail if the service isn't created yet
      - name: Check Cloud Run service (allow not-found)
        shell: bash
        run: |
          set +e
          if gcloud run services describe aurify-ssr --region=me-central1 >/dev/null 2>&1; then
            echo "✔ Cloud Run aurify-ssr present in me-central1"
          else
            echo "ℹ️ Cloud Run service aurify-ssr not found yet in me-central1 (ok before first deploy)"
          fi

      # Uncomment if you want hard checks for Firestore and BigQuery locations (requires exact dataset name)
      # - name: Firestore location check
      #   run: |
      #     LOC=$(gcloud firestore databases describe --database="(default)" --format="value(locationId)")
      #     test "$LOC" = "me-central1" || { echo "❌ Firestore not in me-central1 ($LOC)"; exit 1; }
      #     echo "✔ Firestore in me-central1"
      #
      # - name: BigQuery dataset location check
      #   env:
      #     DATASET: aurify_logs   # TODO: set to your dataset
      #   run: |
      #     LOC=$(bq show --format=prettyjson "$DATASET" | jq -r '.location')
      #     test "$LOC" = "ME-CENTRAL1" || { echo "❌ BQ dataset $DATASET not in ME-CENTRAL1 ($LOC)"; exit 1; }
      #     echo "✔ BigQuery dataset in ME-CENTRAL1"
